/* eslint-disable */
import React from 'react';
import Hero from '../../components/Hero';
import Generate from '../../components/Generate';

const Home = () => (
  <div className="home">
    <Hero />
    <Generate />
  </div>
);

export default Home;
